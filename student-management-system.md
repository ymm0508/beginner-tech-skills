# 模块2：C语言基础编程
- 用结构体定义学生信息
- 实现学生数据的录入和平均分计算
- 将数据写入文本文件

## 项目概述
一个基于C语言开发的学生信息管理系统，具有学生信息录入、平均分计算、数据显示和文件存储等功能。
## 🎯 功能特性
### 核心功能
1. 学生信息录入​ - 录入5个学生的学号、姓名和分数
2. 平均分计算​ - 自动计算并显示学生的平均成绩
3. 信息显示​ - 以表格形式清晰展示所有学生信息
4. 数据持久化​ - 将学生信息保存到 students.txt文件中
5. 交互式菜单​ - 友好的命令行界面，易于操作
### 技术特点
- 使用C语言结构体存储学生数据
- 模块化函数设计，代码结构清晰
- 输入验证机制，确保数据有效性
- 格式化文件输出，便于阅读

## 🧩 代码结构
### 主要函数说明
```
    int main()      //主函数
    void input(Student *s)      // 录入学生信息 - 收集5个学生的数据
    void output(Student *s)      // 计算平均分 - 计算并返回平均分数
    float avaragescore(Student *s, float average)      // 显示学生信息 - 格式化输出所有学生数据
    void writefile(Student *s,float aver)      // 保存到文件 - 将数据写入文本文件
```
